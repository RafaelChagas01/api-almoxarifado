# rodando sem Docker, o padrao e SQLite local
DATABASE_URL=sqlite:///./almoxarifado.db

# gere com: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=

# usado pelo docker-compose
POSTGRES_PASSWORD=

# senha do admin criado por "python -m app.seed" (minimo 16 caracteres)
ADMIN_PASSWORD=
